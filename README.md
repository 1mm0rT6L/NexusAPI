# NexusAPI
So, ```x86_64-w64-mingw32-gcc -o nexus.exe code.c -luser32 -lgdi32 -lwinhttp -mwindows``` compile with this if u do not have compiler in ur windows u can use kali ones.
After that u can create a short cut and change the icon if u want & finally run it.
that's it.
